export { colors } from "./colors";
export { radius } from "./radius";
export { shadows } from "./shadows";
export { spacing } from "./spacing";
export { typography } from "./typography";
// Theme bundle — import `theme` for the full typed design object,
// or `createTheme` to derive a custom variant.
export { activeTheme as theme, createTheme } from "./theme";